# `sim/land`

Parcels, rights and ownership. Home of `LandAuthority.rights_at()`, the one spatial query the whole game asks (design doc §7.1).

**Allowed imports:** `sim/` only.

**Introduced at:** M2 (land authority + starter plot).
