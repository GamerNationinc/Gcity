# `sim/nav`

Route graph (macro), portal graph (player structures), player path traces, and token hydration (design doc §6).

**Allowed imports:** `sim/` only.

**Introduced at:** M3 (portal graph), M7 (route graph).
