# `sim/world`

Chunks, terrain and world generation. Consumes the route graph; never the other way round (design doc §5.2). Terrain representation is blocked on ADR-003.

**Allowed imports:** `sim/` only.

**Introduced at:** M7 (route graph + procgen wilds).
