# `sim/items`

Item templates and instances, weapon frames and sockets, modules, magazines (design doc §11, §8.2, §13.3). Magazine model is blocked on ADR-009.

**Allowed imports:** `sim/` only.

**Introduced at:** M1 (one pistol), M2 (modules).
