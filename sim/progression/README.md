# `sim/progression`

Skills, perk trees and the stat resolver. Every number in the game reads through `StatResolver.get()` (design doc §10.2).

**Allowed imports:** `sim/` only.

**Introduced at:** M1 (stat resolver + one pistol).
