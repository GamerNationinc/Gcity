{
	"schema_version": 1,
	"description": "A stat every entity can carry. Numbers are integers in milli-units; the resolver applies modifiers on top of default_base (design doc §10.2, M1 spec claim 5).",
	"properties": {
		"schema_version": {"type": "int", "min": 1, "max": 1},
		"default_base": {"type": "int", "min": -1000000000, "max": 1000000000},
		"description": {"type": "string", "min_length": 1, "max_length": 240}
	},
	"required": ["schema_version", "default_base", "description"],
	"additional_properties": false
}
