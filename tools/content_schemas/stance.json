{
	"schema_version": 1,
	"description": "A stance an agent can commit to (design doc §14.4; M4 spec claim 10): a registry entry whose scorer is a registration call in the sim. A profile that names a stance with no scorer fails assembly.",
	"properties": {
		"schema_version": {"type": "int", "min": 1, "max": 1},
		"description": {"type": "string", "min_length": 1, "max_length": 240}
	},
	"required": ["schema_version", "description"],
	"additional_properties": false
}
