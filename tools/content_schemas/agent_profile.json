{
	"schema_version": 1,
	"description": "An agent is an actor with a combat profile and a perception profile (M4 spec claim 1). Aim, stress and stance lists join this record in later M4 sessions.",
	"properties": {
		"schema_version": {"type": "int", "min": 1, "max": 1},
		"description": {"type": "string", "min_length": 1, "max_length": 240},
		"combat_profile": {"type": "string", "ref": "combat_profile"},
		"perception_profile": {"type": "string", "ref": "perception_profile"}
	},
	"required": ["schema_version", "description", "combat_profile", "perception_profile"],
	"additional_properties": false
}
