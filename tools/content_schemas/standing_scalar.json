{
	"schema_version": 1,
	"description": "A per-actor standing scalar (design doc §7.3; M6 spec claim 11): how much it cools each tick and where it stops. Milli-units.",
	"properties": {
		"schema_version": {"type": "int", "min": 1, "max": 1},
		"description": {"type": "string", "min_length": 1, "max_length": 240},
		"decay_per_tick": {"type": "int", "min": 0, "max": 1000000},
		"max": {"type": "int", "min": 1, "max": 1000000000}
	},
	"required": ["schema_version", "description", "decay_per_tick", "max"],
	"additional_properties": false
}
